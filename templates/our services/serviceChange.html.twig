{% extends "base.html.twig" %}

{% block title %}Modifier une prestation
{% endblock %}

{% block body %}
<h2>hello
</h2>
            {% for service in services %}
           <a href="{{ path ('serviceForm', {'id': createServiceForm.id}) }}">{{ service }} Prestation à modifier</a> 

                   {# {{ dump(service) }} #}
            {% endfor %}


{% endblock %}
